请根据我提供的知识点列表（每项包含“名称”和“类型”），为每一个知识点生成一段说明文字。要求如下：

1. 说明必须基于该知识点与列表中其他知识点之间的关系来撰写，包括但不限于：前因、背景、参与力量、组成关系、创立、提出、推动、直接结果、深远影响等，按历史逻辑自动判断。
2. 知识来源必须以我提供的课本材料为准，其它资料只能作为辅助，不能和课本材料冲突。
3. 若知识点类型为“事件”，说明中必须包含事件发生的“时间”和“地点”（允许根据常识推断，但不要扩展章节外的内容）。
4. 每段说明使用 30–80 字的自然语言，不要使用项目符号或列表。
5. 输出格式必须是一个 JSON 数组。
6. 数组中的每个元素包含两个字段：
    - "front": "# " + 知识点类型 + "\n\n" + 知识点名称
    - "back": 生成的说明文字
7. 不要输出任何中间推理过程，只输出最终 JSON。

下面是知识点列表，请为每一个生成说明：
（在此粘贴知识点列表，格式如：
武昌起义,事件
中国同盟会,人物与组织
三民主义,历史因素
……）